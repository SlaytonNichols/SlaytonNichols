# UI

Static site frontend for slaytonnichols.com.

## Commands

```bash
npm install
npm run dev
npm run build
npm run serve
```

## Notes

- Markdown posts live in `src/pages/posts/`
- The site is prerendered with `vite-ssg`
- Deployment packages the built output into nginx
